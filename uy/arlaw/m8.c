#include <stdio.h>

int main()
{
                int a,b;
                puts("\t1 - son");
                scanf("%d",&a);
                puts("\t2 - son");
                scanf("%d",&b);

                if(a==b)puts("\tTeng");
                else puts("\tTeng emas");

	    return 0;
}
